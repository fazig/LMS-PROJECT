# LMS Frontend

React (Vite) frontend for the MERN LMS.

## Environment

Create a `.env` file in `frontend/`:

```
VITE_API_URL=http://localhost:5000
```

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
